server {
    listen 80;
    server_name example.com;

    location = /favicon.ico { access_log off; log_not_found off; }
    location /static/ {
        root /home/kris/git_tree/blog;
    }

    location / {
        include         uwsgi_params;
        uwsgi_pass      unix:/tmp/blog.sock;
    }
}
